[alias]
	network = log --all --graph --decorate --oneline
	fa = fetch --all
	fo = fetch origin
	fu = fetch upstream
	pum = pull upstream master
	cm = commit -m
	s = status
	aa = add .
	ai = add -i
	co = checkout
	db = branch -d
